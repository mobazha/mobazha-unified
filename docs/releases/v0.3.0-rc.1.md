# Mobazha Unified v0.3.0-rc.1

Status: release candidate. This version has not been tagged or published.

## Highlights

- One shared frontend `main` branch for self-hosted and hosted Mobazha deployments.
- Backend-authoritative, versioned runtime capability projection.
- Vite and Next.js entry points with the same capability and provider boundaries.
- Default checkout support for BTC, BCH, and LTC; additional compatible backends remain capability-driven.
- Browser wallet integration limited to standards exposed by the browser; connector-specific SDKs are not bundled in the public core.

## Compatibility

- Node.js 20 or newer and pnpm 9.15.4 are required for the supported source build.
- The matching node release candidate is Mobazha v0.3.0-rc.1.
- Optional UI paths remain fail-closed until a valid runtime capability response is available.

## Verification

Before tagging this candidate, release maintainers must run:

```bash
corepack pnpm install --frozen-lockfile
corepack pnpm lint
corepack pnpm typecheck
corepack pnpm --filter @mobazha/core test:unit
corepack pnpm --filter @mobazha/web build
corepack pnpm --filter @mobazha/web build:next
corepack pnpm audit --prod --audit-level=high
```

The final release must also include production-bundle inspection, desktop and mobile browser review, dependency/secret scans, checksums, provenance, and the exact source commit used for every artifact.

## Documentation

- [Release scope](https://docs.mobazha.org/project/release-scope)
- [Runtime capabilities](https://docs.mobazha.org/build/runtime-capabilities)
- [Buyer guidance](https://docs.mobazha.org/buy)
- [Seller guidance](https://docs.mobazha.org/sell)

Before tagging, `pnpm release:docs:check v0.3.0-rc.1` must confirm that
the public documentation source manifest reviews the exact Unified commit and
that the required public routes are available.

## Known limitations

- This release candidate is not a stable production release.
- Runtime capability negotiation requires a compatible Mobazha backend.
- Signed artifacts and reproducibility attestations are pending final release approval.

## Security

Do not report vulnerabilities in a public issue. Follow the private reporting process in [`SECURITY.md`](../../SECURITY.md).
