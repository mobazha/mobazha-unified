/**
 * 占位测试文件
 * @mobazha/ui 中的组件已迁移到 apps/web，此处保留占位测试以确保测试命令正常运行
 */

import { describe, it, expect } from 'vitest';

describe('@mobazha/ui', () => {
  it('包应该正确导出', () => {
    // 这是一个占位测试，确保测试命令正常运行
    expect(true).toBe(true);
  });
});
