---
description: 当用户要求 Code Review 或提交前自动应用
globs:
alwaysApply: false
---

# Code Review 规则

当用户提到以下关键词时，自动进入 Code Review 模式：
- "review"、"审查"、"检查代码"、"code review"
- "帮我看看这个代码"、"这段代码有问题吗"
- "review staged"、"提交前检查"

## Review 流程

### 1. 确认 Review 范围

- 如果是 staged changes，使用 `git diff --staged`
- 如果是整个功能，确认相关文件列表
- 优先 review 最近修改的文件

### 2. Review 维度和权重

| 维度 | 权重 | 检查点 |
|------|------|--------|
| **架构一致性** | 20% | 代码复用、分层架构、类型定义位置 |
| 代码质量 | 20% | 命名、结构、可读性、DRY |
| React 最佳实践 | 15% | Hooks、性能、组件设计 |
| TypeScript | 15% | 类型安全、泛型使用、避免 any |
| 迁移一致性 | 10% | 与原 RN 功能对齐 |
| 响应式设计和间距 | 10% | 断点、布局适配、内边距、元素间距 |
| 错误处理 | 5% | try-catch、用户反馈 |
| 测试覆盖 | 5% | 单元测试完整性 |

### 3. 常见问题检查

#### 架构一致性（重要！详见 [code-reuse-rules.mdc](./code-reuse-rules.mdc)）

```typescript
// ❌ 在 apps/ 中定义业务类型（Props 除外）
// apps/web/src/components/Order/OrderDetailModal.tsx
interface DisplayOrder { ... }  // 应在 @mobazha/core/types/
interface RealOrderData { ... } // 应在 @mobazha/core/types/

// ❌ 在 apps/ 中定义转换函数
function transformCoreOrder() { ... }  // 应在 @mobazha/core/utils/

// ❌ 组件直接调用 API
const order = await ordersApi.getOrder(id);  // 应使用 hooks

// ❌ 相同逻辑在多处实现
// 文件 A: if (peerID === vendorID) role = 'seller';
// 文件 B: if (peerID === vendorID) role = 'seller'; // 重复！

// ✅ 正确做法
import { DisplayOrder, transformCoreOrder } from '@mobazha/core';
const { displayOrder } = useOrderDetail(orderId, viewingContext);
```

#### React/Hooks
```typescript
// ❌ 内存泄漏 - 缺少清理
useEffect(() => {
  const subscription = subscribe();
  // 缺少 return () => subscription.unsubscribe();
}, []);

// ❌ 依赖数组不完整
useEffect(() => {
  fetchData(userId); // userId 不在依赖中
}, []);

// ❌ 不必要的重渲染
<Button onClick={() => handleClick(id)} /> // 应用 useCallback
```

#### TypeScript
```typescript
// ❌ 使用 any
const data: any = response;

// ✅ 使用 unknown + 类型收窄
const data: unknown = response;
if (isProduct(data)) {
  // data 现在是 Product 类型
}
```

#### 响应式设计
```typescript
// ❌ 硬编码尺寸
width: 375 // 应该用百分比或响应式值

// ❌ 缺少响应式处理
// 没有针对不同断点的布局
```

#### UI 间距
```tsx
// ❌ 内边距不足 - 内容紧贴卡片边界
<div className="rounded-lg border p-1">  // 应至少 p-4
  <span>内容</span>
</div>

// ❌ 元素之间缺少间距
<div className="flex items-center">
  <Icon /><span>Text</span>  // 缺少 gap-2
</div>

// ❌ 内容直接贴边
<Card>
  <div>内容直接贴边</div>  // 卡片内应有 p-4
</Card>

// ✅ 正确做法
<div className="rounded-lg border p-4">
  <div className="flex items-center gap-2">
    <Icon />
    <span>Text</span>
  </div>
</div>
```

### 4. 架构一致性检查（必须）

- [ ] `apps/` 中没有业务类型定义（`interface`/`type`，Props 除外）
- [ ] `apps/` 中没有 `transform*` 或 `map*` 函数
- [ ] 组件不直接调用 `xxxApi.*`，而是使用 hooks
- [ ] 相同逻辑没有在多处重复实现
- [ ] 上下文（如 `viewingContext`）正确传递

### 5. 迁移一致性检查

- [ ] 功能与原 RN 实现一致
- [ ] API 调用参数相同
- [ ] 状态管理逻辑相同
- [ ] 错误处理方式相同

### 6. UI 间距检查

- [ ] 卡片组件有足够内边距 (至少 `p-4`)
- [ ] 图标和文字之间有适当间距 (`gap-2`)
- [ ] 按钮距离边界有安全距离
- [ ] 列表项有适当的上下/左右间距
- [ ] 内容不紧贴卡片边界

### 7. 输出 Review 报告

```markdown
## Code Review 报告

### 📁 审查文件
- file1.ts (+X 行)
- file2.tsx (+X 行)

### 📊 评分：X/10

### 🔴 必须修复
#### Issue #1: [标题]
- 位置：`文件:行号`
- 问题：描述
- 建议修复：代码示例

### 🟡 建议改进
1. ...

### ✅ 功能对齐检查
- [x] 与原 RN 逻辑一致
- [ ] 缺少 XX 功能

### ✨ 代码亮点
1. ...

### 结论
- [ ] 可以提交
- [ ] 需要修复后重新 Review
```

## 跟进

- 修复后可以请求 re-review
- 严重问题标记为阻断性

---

**注意**: 此规则会随项目进展动态调整。
