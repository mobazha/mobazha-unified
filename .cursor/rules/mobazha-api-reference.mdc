# Mobazha API 参考规范

本规则提供 Mobazha 后端 API 的参考信息，用于前端开发时校对参数格式。

## 架构概述

```
[前端客户端] → [mobazha_hosting (网关)] → [mobazha3.0 (核心节点)]
                     ↓
              部分 API 直接处理
              部分 API 代理转发
```

- **mobazha_hosting**: 托管服务网关，处理用户管理、群组市场等
- **mobazha3.0**: 核心 P2P 节点，处理订单、商品、钱包等

## 后端源码位置

### mobazha3.0 核心结构

| 数据类型 | 文件路径 | 说明 |
|---------|---------|------|
| Purchase | `pkg/models/purchase.go` | 订单创建请求结构 |
| PurchaseItem | `pkg/models/purchase.go` | 订单商品项 |
| ListingMetadata | `pkg/models/listings.go` | 商品索引（包含 CID） |
| SignedListing | `pkg/orders/mbzpb/` | 商品详情 (protobuf) |
| API 路由 | `internal/api/gateway.go` | API 路由定义 |
| 订单处理 | `internal/api/order_handlers.go` | 订单 API 实现 |
| 商品处理 | `internal/api/listing_handlers.go` | 商品 API 实现 |

### mobazha_hosting 路由

| 功能模块 | 文件路径 |
|---------|---------|
| API 网关 | `api/gateway.go` |
| 用户托管服务 | `host/` |
| 群组市场 | `api/group_marketplace_*.go` |

## API 端点分类

### 订单相关 (`/v1/order/*`, `/v1/ob/*`)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/order/purchase` | POST | 创建订单 |
| `/v1/order/payment` | POST | 提交支付 |
| `/v1/order/confirm` | POST | 卖家确认订单 |
| `/v1/order/fulfill` | POST | 卖家发货 |
| `/v1/order/complete` | POST | 买家确认收货 |
| `/v1/order/cancel` | POST | 取消订单 |
| `/v1/order/refund` | POST | 退款 |
| `/v1/ob/order/{orderID}` | GET | 获取订单详情 |
| `/v1/ob/purchases` | GET | 获取购买订单列表 |
| `/v1/ob/sales` | GET | 获取销售订单列表 |
| `/v1/ob/estimatetotal` | POST | 估算订单总价 |

### 商品相关 (`/v1/ob/*`)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/ob/listing/{slug}` | GET | 获取自己的商品详情 |
| `/v1/ob/listing/{peerID}/{slug}` | GET | 获取他人商品详情 |
| `/v1/ob/listingindex` | GET | 获取自己的商品索引 |
| `/v1/ob/listingindex/{peerID}` | GET | 获取他人商品索引 |
| `/v1/ob/listing` | POST | 创建/更新商品 |
| `/v1/ob/listing/{slug}` | DELETE | 删除商品 |

### 用户/Profile (`/v1/ob/*`)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/ob/profile` | GET | 获取自己的 Profile |
| `/v1/ob/profile` | POST | 更新 Profile |
| `/v1/ob/profile/{peerID}` | GET | 获取他人 Profile |
| `/v1/ob/avatar` | POST | 上传头像 |
| `/v1/ob/images` | POST | 上传图片 |

### 购物车 (`/v1/ob/carts/*`)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/ob/carts` | GET | 获取购物车 |
| `/v1/ob/carts/{peerID}/add` | POST | 添加商品到购物车 |
| `/v1/ob/carts/{peerID}/remove` | POST | 从购物车移除 |
| `/v1/ob/carts` | DELETE | 清空购物车 |

### 钱包 (`/v1/wallet/*`)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/wallet/mnemonic` | GET | 获取助记词 |
| `/v1/wallet/spend` | POST | 发送交易 |
| `/v1/wallet/receivingaccountlist` | GET | 获取收款账户列表 |

## 关键数据结构

### 创建订单请求 (POST `/v1/order/purchase`)

```go
// 后端期望的结构 (models.Purchase)
type Purchase struct {
    ShipTo       string         `json:"shipTo"`       // 收件人姓名
    Address      string         `json:"address"`      // 地址行（字符串，非对象）
    City         string         `json:"city"`
    State        string         `json:"state"`
    PostalCode   string         `json:"postalCode"`
    CountryCode  string         `json:"countryCode"`  // 注意是 countryCode
    AddressNotes string         `json:"addressNotes"`
    Items        []PurchaseItem `json:"items"`
    PricingCoin  string         `json:"pricingCoin"`  // 定价币种
    Moderator    string         `json:"moderator"`    // 仲裁人 PeerID (可选)
    // 注意：没有 vendorId 字段！后端从 listingHash 解析
}

type PurchaseItem struct {
    ListingHash      string   `json:"listingHash"`  // CID，非 slug！
    Quantity         string   `json:"quantity"`     // 字符串类型！
    Options          []Option `json:"options"`
    Shipping         Shipping `json:"shipping"`
    Memo             string   `json:"memo"`
    Coupons          []string `json:"coupons"`
    PaymentAddress   string   `json:"paymentAddress"`
    OptionalFeatures []string `json:"optionalFeatures"`
}
```

**前端注意事项：**
- `listingHash` 必须是 CID（如 `QmXxxx...`），不是 slug
- `quantity` 必须是字符串，不是数字
- 地址是扁平字段，不是嵌套对象
- 没有 `vendorId` 字段

### 商品索引 (GET `/v1/ob/listingindex/{peerID}`)

```go
// 返回 []ListingMetadata
type ListingMetadata struct {
    CID          string   `json:"cid"`          // IPFS CID - 用于 listingHash
    Slug         string   `json:"slug"`
    Title        string   `json:"title"`
    ContractType string   `json:"contractType"` // PHYSICAL_GOOD, DIGITAL_GOOD, SERVICE, RWA_TOKEN
    // ... 其他字段
}
```

**前端注意事项：**
- 商品详情 API 不返回 CID，需要从 listingindex 获取
- CID 每次商品更新后会变化

### 合约类型 (ContractType)

```go
const (
    PHYSICAL_GOOD = "PHYSICAL_GOOD"  // 物理商品 - 需要收货地址
    DIGITAL_GOOD  = "DIGITAL_GOOD"   // 数字商品
    SERVICE       = "SERVICE"        // 服务
    RWA_TOKEN     = "RWA_TOKEN"      // RWA 代币
)
```

## 常见字段映射

| 前端字段 | 后端字段 | 说明 |
|---------|---------|------|
| `address.name` | `shipTo` | 收件人姓名 |
| `address.street` | `address` | 地址行 |
| `address.country` | `countryCode` | 国家代码 |
| `quantity` (number) | `quantity` (string) | 需要转换为字符串 |
| `listingHash` / `cid` | `listingHash` | 必须是 CID |

## 调试技巧

1. **查看后端期望的结构**：
   - 检查 `mobazha3.0/pkg/models/*.go` 中的类型定义
   - 注意 JSON tag 的实际字段名

2. **API 错误排查**：
   - `json: cannot unmarshal...` 表示类型不匹配
   - 检查字段是否需要是字符串/数字/对象

3. **获取 listing CID**：
   - 调用 `/v1/ob/listingindex/{peerID}` 获取
   - 通过 slug 匹配找到对应的 `cid` 字段

## 参考示例

### 创建订单（非物理商品）

```typescript
// 前端请求
const request = {
  items: [{
    listingHash: "QmbB81CKbTqe779Rax7hXXh8HEHQ9BSfxvSSHPDQZLLJYw", // CID
    quantity: "1",  // 字符串
    memo: "order note"
  }],
  pricingCoin: "USD"
};
// 不发送地址字段
```

### 创建订单（物理商品）

```typescript
// 前端请求
const request = {
  items: [{
    listingHash: "QmXxxxx...",
    quantity: "2",
    shipping: { name: "Standard", service: "USPS" }
  }],
  // 地址是扁平字段
  shipTo: "John Doe",
  address: "123 Main Street",
  city: "San Francisco",
  state: "CA",
  postalCode: "94102",
  countryCode: "US",
  pricingCoin: "USD"
};
```
