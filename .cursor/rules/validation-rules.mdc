---
description: 代码验证规范 - 提交前必须执行的验证步骤
alwaysApply: true
---

# 代码验证规范

## 提交前验证清单

每次提交代码前，必须按顺序执行以下验证步骤：

### 1. TypeScript 类型检查

```bash
cd packages/core && pnpm exec tsc --noEmit
```

### 2. 代码风格检查

```bash
pnpm --filter @mobazha/core lint
```

### 3. 单元测试

```bash
pnpm --filter @mobazha/core test
```

### 4. 完整构建验证（推荐）

```bash
pnpm build
```

> 完整构建能捕获导入/导出错误、路由问题等开发模式下可能遗漏的问题

## 常见问题处理

### Next.js 缓存问题

当遇到模块导出错误但文件确实存在时：

```bash
cd apps/web && rm -rf .next && pnpm dev
```

### Core 包更新后

修改 `packages/core` 后需重新构建：

```bash
pnpm --filter @mobazha/core build
```

然后重启 web 开发服务器

## 快捷命令

```bash
# 快速验证（lint + tsc）
pnpm validate:quick

# 完整验证（lint + tsc + test + build）
pnpm validate
```

## 代码架构检查

### 5. 代码复用检查（新功能/重构时）

在添加新功能或重构代码时，检查是否遵循 [code-reuse-rules.mdc](./code-reuse-rules.mdc)：

```bash
# 检查 apps/ 中是否有不该存在的类型定义（Props 除外）
rg "^(export )?(interface|type) " apps/web/src --include "*.tsx" --include "*.ts" | grep -v "Props"

# 检查是否有重复的转换函数
rg "function transform|function map" apps/web/src

# 检查组件是否直接调用 API
rg "Api\." apps/web/src/components
```

如果发现问题，应将相关代码移到 `packages/core/` 中。

## AI 助手验证要求

在完成代码修改后，AI 助手应：

1. 运行 TypeScript 检查确认无类型错误
2. 运行 lint 检查确认代码风格
3. 如果修改了 `packages/core`，重新构建该包
4. **检查代码架构一致性**（参考 code-reuse-rules.mdc）：
   - 类型定义是否在 `@mobazha/core/types/`？
   - 转换函数是否在 `@mobazha/core/utils/`？
   - 是否有重复代码需要合并？
5. 提交前确认所有验证通过
