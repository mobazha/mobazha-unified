---
description: 组件开发规则 - 创建或修改 UI 组件时应用
globs:
  - packages/ui/**/*.tsx
  - apps/web/components/**/*.tsx
  - apps/web/src/components/**/*.tsx
alwaysApply: false
---

# 组件开发规则

## UI 组件库

项目使用 UI 组件库：

1. **`@/components/ui`** - shadcn/ui 组件 (Dialog, Select, Toast 等)

原来的@mobazha/ui尽量用成熟的组件库进行替换，比如shadcn/ui 等

### 组件选择指南

| 场景 | 使用 |
|------|------|
| 表单控件 (Input, Select, Switch) | `@/components/ui` |
| 对话框 (Dialog, AlertDialog) | `@/components/ui` |
| 消息通知 (Toast) | `@/components/ui` |
| 业务卡片 (ProductCard, OrderCard) | `@mobazha/ui` |
| 布局组件 (Container, Grid) | `@mobazha/ui` |

详细指南请参考: [UI 组件库使用指南](../../docs/features/ui-components.md)

## 组件结构

### 目录结构

```
packages/ui/components/
├── ProductCard/
│   ├── index.tsx           # 导出
│   ├── ProductCard.tsx     # 主组件
│   ├── ProductCard.test.tsx # 测试
│   └── types.ts            # 类型 (可选)
```

### 组件模板

```tsx
'use client';

import { memo } from 'react';
import { cn } from '@/lib/utils';

export interface ProductCardProps {
  /** 商品数据 */
  product: Product;
  /** 点击回调 */
  onClick?: (product: Product) => void;
  /** 自定义样式 */
  className?: string;
}

/**
 * 商品卡片组件
 * 
 * @example
 * <ProductCard product={product} onClick={handleClick} />
 */
export const ProductCard = memo(function ProductCard({
  product,
  onClick,
  className,
}: ProductCardProps) {
  return (
    <article
      className={cn('rounded-lg border p-4', className)}
      onClick={() => onClick?.(product)}
      data-testid="product-card"
    >
      {/* 组件内容 */}
    </article>
  );
});
```

## 响应式设计

### 使用 Tailwind 响应式类

```tsx
// ✅ 好的做法
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4">

// ❌ 避免硬编码
<div style={{ gridTemplateColumns: 'repeat(4, 1fr)' }}>
```

### 使用 useBreakpoint Hook

```tsx
import { useBreakpoint } from '@mobazha/ui/hooks';

function ResponsiveComponent() {
  const { isMobile, isTablet, isDesktop } = useBreakpoint();
  
  if (isMobile) {
    return <MobileLayout />;
  }
  
  return <DesktopLayout />;
}
```

## 性能优化

### 使用 memo

```tsx
// 对于接收对象 props 的组件
export const ProductCard = memo(function ProductCard(props) {
  // ...
});
```

### 使用 useCallback

```tsx
// 传递给子组件的回调函数
const handleClick = useCallback((id: string) => {
  // ...
}, [dependency]);
```

### 避免内联对象/函数

```tsx
// ❌ 每次渲染都创建新对象
<Component style={{ color: 'red' }} />

// ✅ 使用 className 或 useMemo
<Component className="text-red-500" />
```

## 可访问性 (a11y)

### 必须添加的属性

```tsx
// 可点击元素
<button aria-label="Add to cart">
<a href="#" aria-label="View product">

// 图片
<img alt="Product thumbnail" />

// 表单
<input aria-label="Search" />
<label htmlFor="email">Email</label>
```

### 测试 ID

```tsx
// 用于 E2E 测试
<div data-testid="product-card">
<button data-testid="add-to-cart-button">
```

## 样式规范

### 使用 cn 工具合并类名

```tsx
import { cn } from '@/lib/utils';

<div className={cn(
  'base-styles',
  isActive && 'active-styles',
  className
)}>
```

### 设计 Token

使用 Tailwind 的设计 Token，不要硬编码颜色/尺寸：

```tsx
// ✅ 使用 Token
<div className="text-primary bg-background">

// ❌ 硬编码
<div style={{ color: '#00BCD4' }}>
```

## 间距规范 (Spacing)

### 卡片组件内边距

| 元素 | 最小内边距 | Tailwind 类 |
|------|-----------|-------------|
| 卡片容器 | 16px | `p-4` |
| 复杂卡片 | 20-24px | `p-5` / `p-6` |
| 列表项 | 12-16px | `p-3` / `p-4` |
| 按钮内边距 | 8-12px 水平 | `px-3` / `px-4` |

### 元素间距

| 场景 | 最小间距 | Tailwind 类 |
|------|---------|-------------|
| 图标与文字 | 8px | `gap-2` |
| 按钮组 | 8-12px | `gap-2` / `gap-3` |
| 表单元素 | 16px | `space-y-4` |
| 段落/区块 | 16-24px | `space-y-4` / `space-y-6` |

### 边界安全区

内容不应紧贴卡片边界，最小安全距离：

| 元素类型 | 最小距离 |
|---------|---------|
| 文字内容 | 16px |
| 图标/按钮 | 12px |
| 复选框等交互元素 | 16px |

### 间距检查示例

```tsx
// ✅ 好的做法 - 卡片有足够内边距
<div className="rounded-lg border p-4">
  <div className="flex items-center gap-2">
    <Icon />
    <span>文字内容</span>
  </div>
</div>

// ✅ 好的做法 - 列表项有适当间距
<div className="space-y-4">
  <ListItem className="p-4" />
  <ListItem className="p-4" />
</div>

// ❌ 避免 - 内边距太小
<div className="rounded-lg border p-1">...</div>

// ❌ 避免 - 缺少元素间距
<div className="flex items-center">
  <Icon /><span>Text</span>  {/* 应该加 gap-2 */}
</div>

// ❌ 避免 - 内容直接贴边
<Card>
  <div>内容直接贴边</div>  {/* 应该在 Card 内加 p-4 */}
</Card>
```

## 测试要求

### 必须测试

- 渲染正确
- Props 传递
- 事件处理
- 响应式行为 (关键组件)

### 测试示例

```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { ProductCard } from './ProductCard';

describe('ProductCard', () => {
  const mockProduct = {
    id: '1',
    title: 'Test Product',
    price: 99.99,
  };

  it('renders product info', () => {
    render(<ProductCard product={mockProduct} />);
    expect(screen.getByText('Test Product')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const onClick = vi.fn();
    render(<ProductCard product={mockProduct} onClick={onClick} />);
    fireEvent.click(screen.getByTestId('product-card'));
    expect(onClick).toHaveBeenCalledWith(mockProduct);
  });
});
```

---

## 多端用户体验规范

为确保移动端体验接近原 mobazha-mobile，桌面端体验高质量，请遵循以下规范。

> 详细规则请参考：
> - [mobile-ux-rules.mdc](./mobile-ux-rules.mdc) - 移动端用户体验规则
> - [desktop-ux-rules.mdc](./desktop-ux-rules.mdc) - 桌面端用户体验规则

### 移动端紧凑模式

移动端应使用更紧凑的间距和字体，以提高信息密度：

```tsx
// ✅ 移动端紧凑写法
<Card className="p-3">                    {/* 12px 内边距 */}
  <h2 className="text-base font-semibold"> {/* 16px 标题 */}
  <p className="text-sm">                  {/* 14px 正文 */}
  <span className="text-xs text-muted-foreground"> {/* 12px 辅助 */}
</Card>

// ❌ 避免 - 移动端过大的间距和字体
<Card className="p-4 space-y-4">
  <h2 className="text-xl">
  <p className="text-base">
</Card>

// ✅ 响应式写法 - 移动端紧凑，桌面端正常
<Card className="p-3 md:p-4">
  <h2 className="text-base md:text-lg">
  <p className="text-sm md:text-base">
</Card>
```

### 交互反馈工具类

使用 `globals.css` 中定义的工具类：

```tsx
// 触摸反馈 - 点击时轻微缩小
<button className="touch-feedback">点击我</button>

// 列表项触摸反馈 - 点击时背景变化
<div className="touch-feedback-bg">列表项</div>

// 悬停提升效果（桌面端）
<Card className="hover-lift">卡片</Card>

// 悬停背景高亮（桌面端）
<div className="hover-highlight">列表项</div>
```

### 触摸目标尺寸

确保所有可点击元素满足 44x44pt 最小触摸目标：

```tsx
// ✅ 使用工具类
<button className="touch-target">按钮</button>

// ✅ 使用 Tailwind
<button className="min-h-11 min-w-11">按钮</button>

// ✅ 内联元素只确保高度
<a className="touch-target-inline">链接</a>
```

### 响应式间距快捷类

```tsx
// 响应式内边距
<div className="p-responsive">...</div>      {/* 移动端 12px，桌面端 16px */}
<div className="py-responsive">...</div>     {/* 移动端 10px，桌面端 12px */}
<div className="px-responsive">...</div>     {/* 移动端 12px，桌面端 16px */}

// 响应式间距
<div className="gap-responsive">...</div>    {/* 移动端 12px，桌面端 16px */}

// 响应式字体
<p className="text-responsive">...</p>       {/* 移动端 14px，桌面端 16px */}
<span className="text-responsive-sm">...</span> {/* 移动端 12px，桌面端 14px */}
<h1 className="text-responsive-lg">...</h1>  {/* 移动端 18px，桌面端 20px */}
```

### 动画效果

```tsx
// 淡入
<div className="animate-fade-in">内容</div>

// 从下滑入
<div className="animate-slide-up">内容</div>

// 从右滑入
<div className="animate-slide-in-right">内容</div>

// 缩放弹入
<div className="animate-scale-in">内容</div>
```

### 桌面端悬停效果

桌面端所有可交互元素必须有悬停状态：

```tsx
// 卡片 - 阴影增强 + 上浮
<Card className="hover:shadow-lg hover:-translate-y-0.5 transition-all">

// 按钮 - 背景色变化
<Button className="hover:bg-primary/90">

// 列表项 - 背景高亮
<div className="hover:bg-muted/50 transition-colors">

// 链接 - 颜色 + 下划线
<a className="hover:text-primary hover:underline">
```

### 快速检查清单

**移动端：**
- [ ] 正文 `text-sm` (14px)，标题 `text-base`/`text-lg`
- [ ] 卡片内边距 `p-3` (12px)
- [ ] 列表项 `py-2.5`/`py-3`
- [ ] 触摸目标最小 44px
- [ ] 有 `active:` 触摸反馈

**桌面端：**
- [ ] 所有可交互元素有 `hover:` 状态
- [ ] 支持键盘 Tab 导航
- [ ] 聚焦状态可见 `focus-visible:ring-2`
- [ ] 网格布局 4+ 列

---

**注意**: 此规则会随项目进展动态调整。
